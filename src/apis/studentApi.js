import axios from "axios";

const prefix = "student";

const GET_ALL_STUDENT = "get-all-student";

export const updateStudentById = async (body) => {
    try {
        
    } catch (error) {
        
    }
};
